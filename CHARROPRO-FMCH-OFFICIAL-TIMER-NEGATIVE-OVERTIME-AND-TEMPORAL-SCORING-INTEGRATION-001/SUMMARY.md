# Summary

The Official Timer Authority now preserves elapsed time beyond the certified duration and derives signed `remainingMs`, `overtimeMs`, `overtime`, and semantic `alertState` without Firebase writes per tick.

Phone control, Scorer, Graphics, Timer Display, Broadcast Data Contract, Output Routing, and Announcer use the shared live display derivation or its sanitized projection. Overtime is rendered in red from semantic state, not from formatted text.

Objective temporal consequences are materialized at the publication boundary through existing RuleIDs. Attempt V2 preserves signed timing evidence. Official Score and Official Format continue using the existing 239 FieldID contract.

Manganas unused-time points require a valid mangana and an objectively completed sequence. Intermediate opportunities preserve evidence but cannot duplicate the sequence bonus. Refresh, reconnect, and retry recompute the same deterministic RuleID quantities.

Coleadero remains a 20-second `FLOW_ONLY` timer with independent opportunity identity. Piales keeps participant-specific previous-opportunity resolution. Toro still prepares Terna as READY without auto-start. Terna participant row ownership is unchanged.
