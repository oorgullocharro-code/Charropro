# Files Changed

## Functional

- `js/core/firebaseSync.js`: skips only the redundant manual reset for fresh
  `PENDING` jobs; preserves reset behavior for recoverable non-pending states.
- `tests/public-live-feed-integration.test.mjs`: covers `PENDING` direct claim,
  `DEAD_LETTER`/`FAILED`/`RETRY_WAIT` reset paths, retry-all scoping, and
  protected statuses.

## Mechanical

- `functions/configuration.defaults.json`: canonical client build identity and
  derived checksum/fingerprint.
- Runtime and test imports: canonical cache-buster propagation generated by
  `tools/release/applyClientBuildVersion.mjs`.
- Release-identity expectations and `PRODUCTION_NAV_V1.md`: updated to the
  same canonical build.

## Evidence

- This ticket directory records the diagnosis, validation, and deployment
  boundary. No local Emulator export, temporary file, credential, Rules file,
  or Function source is included.
