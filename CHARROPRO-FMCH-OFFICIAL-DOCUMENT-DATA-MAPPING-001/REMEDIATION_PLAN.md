# Remediation Plan

1. P0 sports validation: approve controls, Cala terms, Coleadero cardinality, Terna topology, time and closing formula.
2. P0 data remediation: persist only approved field relations and versioned roster/header/document snapshots.
3. P0 scoring compliance: retain granular approved inputs and formula inputs.
4. P0 export engine: select an immutable official document revision, never mutable current state.
5. P1 XLSX/PDF/print: render fieldId-bound approved layout and institutional profile.
6. P1 tests: golden files and 239-cell regression checks.

Guardrails: do not infer values, use visual rows as authority, backfill history as if captured, or alter rules before approval.
