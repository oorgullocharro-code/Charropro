# Copy this file to .env.local, .env.staging, or .env.production as appropriate.
# Do not commit the copied file. Values marked REPLACE must come from an
# authorized environment, never from production defaults.
CHARROPRO_ENV=local
FIREBASE_PROJECT_ID=demo-charropro-local
