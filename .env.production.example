# Production values are provisioned and injected only by authorized release work.
# This template contains no production identifiers or credentials.
CHARROPRO_ENV=production
FIREBASE_PROJECT_ID=REPLACE_WITH_AUTHORIZED_PRODUCTION_PROJECT_ID
FIREBASE_DATABASE_URL=https://REPLACE_WITH_AUTHORIZED_PRODUCTION_DATABASE_URL
FIREBASE_STORAGE_BUCKET=REPLACE_WITH_AUTHORIZED_PRODUCTION_BUCKET
