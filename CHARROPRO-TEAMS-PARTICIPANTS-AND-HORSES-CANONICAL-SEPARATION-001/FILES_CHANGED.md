# Files Changed

- `js/core/state.js`: canonical registries and individual lot/scorer resolvers.
- `js/core/firebaseSync.js`: tournament-state persistence for participants,
  horses, and participant references.
- `js/app.js` and `css/styles.css`: individual registration, horse selection,
  participant lots, and display.
- `js/core/canonicalTournamentResults.js` and public V3 modules: canonical
  participant/horse projection, mirrored in Functions.
- `tests/individual-competition-participant-source.test.mjs`: directed registry,
  scorer, persistence, and V3 parity coverage.
